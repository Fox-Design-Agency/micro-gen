package middleware

// ReturnGoMiddleware will return the byte slice of the generic middleware file
func ReturnGoMiddleware() ([]byte, error) {
	return []byte(`PUT MIDDLEWARE FILES HERE - INTEGRATION COMING SOON`), nil
}
